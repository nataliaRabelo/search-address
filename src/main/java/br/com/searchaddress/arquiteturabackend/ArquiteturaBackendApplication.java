package br.com.searchaddress.arquiteturabackend;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * @author Natália Bruno Rabelo.
 * Classe que implementa a lógica principal da aplicação.
 */
@SpringBootApplication
public class ArquiteturaBackendApplication {

	/**
	 * Lógica da main.
	 * @param args Argumentos de entrada como linhas de comando.
	 */
	public static void main(String[] args) {
		SpringApplication.run(ArquiteturaBackendApplication.class, args);
	}
}